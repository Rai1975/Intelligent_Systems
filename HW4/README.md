## Preface
There are two files for this assignment. `general_ffn.ipynb` and `auto-encoder.ipynb`. Instead of using one file, I segregated them into two separate python notebooks to keep training and figures clean.
`general_ffn.ipynb` contains code for part 1 of the assignment, and `auto-encoder.ipynb` contains code for the second part of the assignment.

---
## Instructions to Run
1. Make sure the data files are stored in the same directory under a folder titled `data`.
2. Run `pip install matplotlib numpy`
3. Run each cell of the notebook to run the program.